<!doctype html>

<html>

<head>
  <meta charset="utf-8">

  <% if @item[:author] %>
  <meta name="description" content="<%= description(@item) %>" />
  <% end %>

  <meta name="twitter:card" content="summary">
  <meta name="twitter:creator" content="@<%= @site.config[:author_twitter] %>">
  <meta name="twitter:title" content="<%= page_title(@item) %>">
  <meta name="twitter:description" content="<%= description(@item) %>">
  <% if @item[:image_url] %><meta name="twitter:image" content="<%= @item[:image_url] %>">
  <meta property="og:image" content="<%= @item[:image_url] %>"><% end %>
  <meta property="og:type" content="article">
  <meta property="og:url" content="<%= @site.config[:base_url] + @item.path %>">
  <meta property="og:site_name"	 content="<%= @site.config[:title] %>">

  <meta property="og:title"	 content="<%= page_title(@item) %>">
  <meta property="og:description"  content="<%= description(@item) %>">
  <link rel="stylesheet" href="/css/style.css">
  <title><%= @site.config[:title] %>: <%= page_title(@item) %></title>
  <script src="//ajax.googleapis.com/ajax/libs/jquery/1.7.1/jquery.min.js"></script>
  <script src="//ajax.googleapis.com/ajax/libs/jqueryui/1.8.16/jquery-ui.min.js"></script>
  <script src="/js/master.js"></script>
  <!-- Google Analytics -->
  <script>
    (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
    (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
    m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
    })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

    ga('create', 'UA-26426915-2');
    ga('send', 'pageview');

  </script>
  <!-- End Google Analytics -->
  <script type="text/javascript" src="//use.typekit.net/myc7idl.js"></script>
  <script type="text/javascript">try{Typekit.load();}catch(e){}</script>
</head>

<body>
  <div id="main">
    <header>
    <a class="logo" href="/"><img src="/img/tsr.png"></a>
    <div class="extras">
      <%= ext_link('<img src="/img/rss-25.png">', @site.config[:feedburner_url], :title => 'Subscribe via RSS') %>
      <%= ext_link('<img src="/img/itunes.png">', @site.config[:itunes_url], :title => 'Add the podcast in iTunes') %>
    </div>
    </header>

    <%= yield %>
    <div class="push"></div>
  </div>

  <footer>
      <h2><i>The Spoken Rune</i> is a collection of verse and prose read aloud.</h2>
      <ul>
        <li><%= ext_link('Subscribe via RSS', @site.config[:feedburner_url], :title => 'Subscribe via RSS') %></li>
        <li><%= ext_link('Add the podcast in iTunes', @site.config[:itunes_url], :title => 'Add the podcast in iTunes') %></li>
      </ul>

      <p>These readings are intended to educate on poetry and literature, aid in the discovery of new works, and celebrate their creators.  If you believe in good faith that materials posted here infringe your copyright, you (or your agent) can contact me requesting that I remove the material.</p>
      <p>All text read by <%= ext_link(@site.config[:author_name], @site.config[:author_webpage], :title => @site.config[:author_name]) %>.</p>
      <p>Site artwork and symbols by <%= ext_link('Marcelo Gallegos', @site.config[:artist_webpage], :title => 'Marcelo Gallegos') %>.</p>
      <p>Contact me!  Requests, critiques, suggestions, sonnets, sestinas, triolets, rondels, limericks, <i>etc</i>.
        <ul>
          <li><a href="mailto:<%= @site.config[:author_email] %>">Email</a></li>
          <li><%= ext_link('Twitter', "http://twitter.com/#{@site.config[:author_twitter]}", :title => 'Twitter') %></li>
          <li><%= ext_link('Google+', @site.config[:author_gplus], :title => 'Google Plus') %></li>
          <li><%= ext_link('Soundcloud', @site.config[:soundcloud_url], :title => 'Soundcloud') %></li>
        </ul>
      </p>
  </footer>
</body>

</html>
